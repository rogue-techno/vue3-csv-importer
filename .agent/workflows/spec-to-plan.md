---
description: Standard process for converting a spec into an implementation plan
---

1. Read the user's request, the README.md and any `*.SPEC.md` file.
2. Check existing `components/` to see if reusable UI exists.
3. Create an `implementation_plan.md` artifact.
4. Wait for user approval before writing code.